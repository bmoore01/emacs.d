;;; elixir -- Summary
;; All configuration for the elixir programming language

;;; Commentary:

;;; Code:
(add-to-list 'exec-path (concat user-emacs-directory "language-servers/elixir-ls"))

;;(use-package elixir-mode
;;  :hook ('elixir-mode-hook . 'flycheck-mode))

(provide 'elixir)
;;; elixir.el ends here
